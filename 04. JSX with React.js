// JSX with React 
// - JSX : JavaScript XML
// - JSX is simply using HTML with JavaScript 
// - We don't need to install anything from outside for using JSX
// - We can also use React without using JSX but it becomes more complicate 
// Interview Question :-
// Q. Can we use React and Angular in same project?
// Ans : Yes, As React is library it can use in any project

function User() {
    return (
        <div>
            <div>Hello</div>
            <div>{10 + 20}</div>
        </div>
    )
}

export default User;
